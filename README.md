I think this might be a helpful boilerplate unless something else exists.

##Add an empty folder "mysql" into the root

##Run docker-compose build

##Run docker-compose up -d

Your local server is good to go